/**
 * Machine3D — Deprecated.
 * Individual machine geometry is no longer rendered.
 * The factory view uses the Sketchfab refinery embed in FactoryPage.jsx.
 */
export default function Machine3D() {
  return null;
}
